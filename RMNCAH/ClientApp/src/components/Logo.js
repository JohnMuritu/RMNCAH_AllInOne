const Logo = (props) => (
  <img
    alt="Logo"
    // src="/static/logo.svg"
    src="/static/goldstar_logo.svg"
    // src="/static/logo.jpeg"
    style={{
      width: 180
    }}
    {...props}
  />
);

export default Logo;
